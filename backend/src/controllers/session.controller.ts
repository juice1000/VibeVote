function updateSessions(sessionsObjects: any[]) {
  setInterval(() => {
    console.log('works');

    // fetch the cache object
    // scan through the cache object
    // delete entries that are older than timelimit
  }, 10000);
}

export { updateSessions };
